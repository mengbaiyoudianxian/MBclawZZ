# Runtime

放母体运行时、事件循环、状态机、任务调度与执行编排。

## 主要内容
- 事件循环：消息进入、处理、回复、结束。
- 状态机：会话状态、任务状态、失败状态。
- 调度器：把任务分发给合适的执行单元。
- 编排层：协调 Memory、Tool、Agent、Gateway 的协作。

## 设计原则
- Runtime 负责调度，不直接替代业务。
- 事件驱动优先。
- 状态变化要可追踪、可回放。
