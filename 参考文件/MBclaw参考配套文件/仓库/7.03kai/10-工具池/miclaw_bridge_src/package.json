{
  "name": "miclaw_api_bridge",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "preview": "vite preview",
    "build:server": "pnpm build && cd src-tauri && cargo build --release --bin miclaw_api_bridge",
    "build:desktop": "pnpm build && cd src-tauri && cargo build --release --features desktop --bin miclaw_api_bridge_desktop",
    "build:binaries": "bash scripts/build-binaries.sh"
  },
  "dependencies": {
    "pinia": "^2.2.4",
    "vue": "^3.5.12",
    "vue-router": "^4.4.5"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.1.4",
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "vue-tsc": "^2.1.10"
  }
}
