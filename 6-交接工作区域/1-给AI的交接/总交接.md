# 总交接

## 一句话结论
当前工作区已经从“搭目录”推进到“收口边界 + 合并模型入口口径 + 形成可接手结论”。下一窗口可以直接顺着 `main` 继续做文档收口，不需要重新盘点起步。

## 当前状态
- 本地仓库：`/tmp/openhands-sandboxes/MBclawZZ/`
- 当前默认分支：`main`
- 已合并完成：`mbclawzz-doc-sync` → `main`
- 已推送 GitHub：是
- 最新可追踪提交：`d9f4e005`
- 这次没有创建 PR

## 已完成的关键工作
1. 建好 MBclawZZ 的总工作区骨架。
2. 盘点账号 `mengbaiyoudianxian` 下的 GitHub 仓库。
3. 确认最新主线仓库是 `mbclaw-mother`。
4. 建立并补强总览、对标、研究、健康检查、交接等入口文档。
5. 把模型入口相关整理主载体收口到 `sub2api`。
6. 把后端目录中的 `TokenPool` 入口改成 `sub2api` 入口。
7. 新增并合并了两份核心专题文档：
   - `4-总览端/3-对标矩阵/MBclaw_to_sub2api_专题整理大纲.md`
   - `4-总览端/3-对标矩阵/mbclaw-mother_与_sub2api_职责边界.md`
8. 已把当前工作分支合并进 `main` 并推送远端。

## 当前已形成的核心口径
- **主线仍然是 `mbclaw-mother`**。
- **sub2api 是第一层地基，Mother 是建立在 sub2api 之上的第二层地基**。
- **控制面板必须建立在 sub2api 与 Mother 两层地基之上，并提前预留三端与 Web 所需 API、数据上传接口与状态接口**。
- **TokenPool 只保留为历史参考，相关功能目标已经在工作区里收口为 sub2api 口径**。
- **兼容方向固定为上层兼容下层地基，而不是让 sub2api 反向兼容 Mother 历史接口**。
- **OpenClaw / OpenHands / LiteLLM / mem0 / MCP 按“能直接照搬就直接照搬，不能照搬再裁剪”处理**。
- **`MBclawZZ` 只是总工作区，不是业务实现仓库**。

## 已同步的结构变化
- `2-后端/4-TokenPool后端/` → `2-后端/4-sub2api后端/`
- 对标矩阵入口已更新
- 资产总览、交接摘要、研究记录等活跃文档已统一到 sub2api 口径
- 参考资料区和历史分析区保留原样，不作为本轮合并目标

## 当前最值得继续做的事
1. 继续把 `必读大纲.md` 压缩成更干净的接手版。
2. 继续收敛 `4-总览端/3-对标矩阵/` 里的专题文档，保留真正可执行的内容。
3. 检查 `2-后端/`、`4-总览端/`、`6-交接工作区域/` 是否还有残留的旧 TokenPool 口径。
4. 如需进一步合并代码层，再去 `mbclaw-mother` / `sub2api` 的真实仓库做代码侧对齐。

## 风险点
- 参考资料里仍然会保留大量 TokenPool 历史分析，这是正常的，不代表没改。
- 当前工作区里最容易混淆的是“历史参考”和“活跃文档”，后者才是本轮重点。
- 不要再把模型入口口径写成两套并行实现。

## 给下一窗口的最短接手方式
1. 先看 `必读大纲.md`。
2. 再看 `6-交接工作区域/1-给AI的交接/总交接.md`。
3. 再看 `4-总览端/3-对标矩阵/MBclaw_to_sub2api_专题整理大纲.md`。
4. 最后看 `4-总览端/3-对标矩阵/mbclaw-mother_与_sub2api_职责边界.md`。

这样就可以直接继续做收口，不需要重新起盘。