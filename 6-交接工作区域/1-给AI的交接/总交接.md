# 总交接

## 一句话结论
当前主线已经明确收口到：**只做 sub2api 作为 MBclaw 的第一层地基**。活跃代码链已完成第一轮接入收口，下一窗口不要再扩散研究，只继续沿着 `参考文件/GitHub精品参考/sub2api/` 做最小必要魔改。

## 给下一窗口的唯一一句话
**强制把工作区锁到 `/tmp/openhands-sandboxes/MBclawZZ/`，不要用任何会话隔离副本路径；先读 `AGENTS.md` 和本文件，再只围绕 `参考文件/GitHub精品参考/sub2api/` 做地基魔改，严格延续提交 `feb690b45e3ee7bc98aa5995529fdbb3e86bf1d2` 之后的工作，不要扩散到无关研究。**

## 当前状态
- 强制主工作区：`/tmp/openhands-sandboxes/MBclawZZ/`
- 禁止工作区：`/tmp/openhands-sandboxes/<session>/MBclawZZ/` 这类会话副本路径
- 当前默认分支：`main`
- 已推送 GitHub：是
- 最新提交：`feb690b4` — `docs: tighten AI handoff for sub2api foundation`
- 上一个关键提交：`3a2e4adb` — `refactor: route mother model entry through sub2api`
- 当前工作树状态：有本轮 sub2api 本体改动待提交（健康检查 `/v1/health`、MBclaw admin 预留 API、MBclaw 管理页入口、交接记录）

## 当前主线口径
- **sub2api 是第一层地基。**
- **Mother 是建立在 sub2api 之上的第二层地基。**
- **控制面板只能建立在前两层地基之上，不反向定义地基。**
- **兼容方向固定为上层兼容下层，不让 sub2api 反向兼容 Mother 历史接口。**
- **当前任务只做 sub2api 地基，不继续发散做全局研究。**
- **TokenPool 只保留为历史参考，不再作为需要保留兼容壳的目标实现。**

## 这轮已经完成的关键进度
1. 已把 MBclaw 活跃模型入口链收口到 `server_app.sub2api_client.Sub2APIClient`。
2. 已把 `MBclaw-Server` 活跃入口模块中的 `app.*` 旧命名空间导入改成 `server_app.*`，避免运行时找不到包。
3. 已把根入口 `MBclaw-Server/main.py` 里的 `gateway_chat` 从直连 provider 改成走 `Sub2APIClient`。
4. 已把 `server_app/api.py`、`pipeline.py`、`agent.py`、`providers.py`、`db.py`、`models.py`、`memory.py`、`tools.py`、`server_app/main.py`、`server_app/admin/admin_api.py` 做了活跃链修正。
5. 已跑过基础验证：
   - `py_compile`
   - `import main`
   - `import server_app.main`
   - `import server_app.api`
   - `import server_app.agent`
   - `import server_app.pipeline`
   - `import server_app.providers`
   全部通过。
6. 已把这轮活跃链收口推到 `main`，对应提交 `3a2e4adb`。

## 本轮 feb690b4 之后新增进展
1. 已只围绕 `参考文件/GitHub精品参考/sub2api/` 开始做本体地基魔改，没有扩散到无关研究。
2. 已确认并修复 Mother 侧 `Sub2APIClient.health()` 默认访问 `/v1/health` 的缺口：`backend/internal/server/routes/common.go` 新增 `/v1/health`，保留 `/health`；`common_test.go` 覆盖两条健康路径。
3. 已新增 MBclaw 管理命名空间：`/api/v1/admin/mbclaw/*`，通过现有 admin JWT / admin API key 鉴权，不另起旧 TokenPool 壳。
4. 已预留并实现 `POST /api/v1/admin/mbclaw/upstream-tokens`：接收 MBclaw 用户上传 key，把 key 写入 sub2api account；支持 `platform`、`base_url`、`model/models`、`provider`、`group_ids`、`owner_user_id`、`concurrency`、`rate_multiplier`、`load_factor`、`priority`、`extra`。
5. 已预留并实现 `POST /api/v1/admin/mbclaw/token-pool-modules`：注册 miclaw 免费代理、商业中转站或其他 OpenAI-compatible token 池模块，作为 sub2api upstream account 承接调度。
6. 已实现 `GET /api/v1/admin/mbclaw/status`：给 Mother 实时报回地基入口、控制入口、token 来源、倍率/并发承接状态、MBclaw 内置 token 面板与 sub2api 管理面板边界。
7. 已确认细节功能的现有承接点：单用户并发用 `users.concurrency`；单用户倍率用 group `rate-multipliers`；单 key 限额/窗口用 API key `quota` 与 `rate_limit_*`；单模型倍率用 channel `model_pricing`；单上游 key 并发/倍率用 account `concurrency` / `rate_multiplier`，默认 1。
8. 已新增前端 `AdminMBclaw` 页面和侧边栏入口“MBclaw 地基”，可查看地基状态、创建上游 key、注册外部 token 池模块。sub2api 原管理面板仍保留 accounts/groups/channels/API keys/usage/ops。
9. 已用临时 Go 1.26.5 工具链验证：`GOPROXY=https://goproxy.cn,direct go test ./internal/server/routes ./internal/handler/admin` 通过。
10. 已验证前端：`./node_modules/.bin/vue-tsc --noEmit` 与 `./node_modules/.bin/vite build` 通过；`pnpm typecheck` 因 pnpm 11 的 ignored-builds 策略会先卡依赖安装检查，直接调用本地 `vue-tsc` 已通过。

## 下一窗口现在应该做什么
只做下面 3 件事：

1. **继续只看 `参考文件/GitHub精品参考/sub2api/` 本体。**
   重点是：
   - `backend/internal/server/routes/`
   - `backend/internal/handler/`
   - `backend/internal/service/`
   - `backend/internal/handler/admin/`
   - 必要时再看 `frontend/` 与 `skills/sub2api-admin/`

2. **继续补齐 MBclaw 对 sub2api 的最小魔改清单。**
   只围绕：
   - 统一模型入口：`/v1/chat/completions`、`/v1/embeddings`、`/v1/health`
   - provider / key 治理：账号、分组、渠道、API key 管理
   - fallback / retry / cooldown / health / stats：复用现有 scheduler、failover、usage、ops 能力
   - 必要的管理接口：优先补“能看见和验证地基状态”的接口，不做大而全控制面板

3. **继续直接改 sub2api 本体，而不是继续改外围说明文档。**
   只有在需要记录阶段结论时，才回写交接文档。

## 现在最该读的文件
按这个顺序读：

1. `AGENTS.md`
2. `6-交接工作区域/1-给AI的交接/总交接.md`
3. `4-总览端/3-对标矩阵/MBclaw_to_sub2api_专题整理大纲.md`
4. `4-总览端/3-对标矩阵/mbclaw-mother_与_sub2api_职责边界.md`
5. `参考文件/MBclaw参考配套文件/仓库/MBclaw-Server/server_app/sub2api_client.py`
6. `参考文件/MBclaw参考配套文件/仓库/MBclaw-Server/main.py`
7. `参考文件/GitHub精品参考/sub2api/README.md`
8. `参考文件/GitHub精品参考/sub2api/backend/`

如果只想最快接手，前 5 个就够了。

## 已确认可复用的代码入口
- `参考文件/MBclaw参考配套文件/仓库/MBclaw-Server/server_app/sub2api_client.py`
- `参考文件/MBclaw参考配套文件/仓库/MBclaw-Server/server_app/llm.py`
- `参考文件/MBclaw参考配套文件/仓库/MBclaw-Server/server_app/api.py`
- `参考文件/MBclaw参考配套文件/仓库/MBclaw-Server/server_app/pipeline.py`
- `参考文件/MBclaw参考配套文件/仓库/MBclaw-Server/server_app/agent.py`
- `参考文件/MBclaw参考配套文件/仓库/MBclaw-Server/server_app/providers.py`
- `参考文件/MBclaw参考配套文件/仓库/MBclaw-Server/main.py`

## 不要再做的事
- 不要再把任务扩成 Runtime Bible 全局研究。
- 不要再去扫一整片无关历史目录找“灵感”。
- 不要再把目标说成“继续兼容 TokenPool”。
- 不要在 `MBclaw-Server` 活跃链上重新引入直连 provider 的入口。
- 不要用会话隔离副本路径当主工作区。

## 风险点
- `参考文件/GitHub精品参考/sub2api/` 是大仓，容易扩散；下一窗口必须只盯和地基有关的目录与能力。
- `MBclaw-Server` 下面仍有历史 `TokenPool` / 调度残留目录，但这轮已经明确：**先不全仓清算，只继续推进 sub2api 地基本体。**
- 之前整仓上传曾被 GitHub secret scanning 卡住；如果后续再次大规模动参考仓快照，先留意是否会引入疑似凭据内容。

## 验收标准
下一窗口做完后，至少要能回答：
1. sub2api 作为地基，MBclaw 最小需要魔改哪几块。
2. 这些魔改分别落在 sub2api 的哪个目录。
3. Mother 现在经过什么入口调用 sub2api。
4. 哪些旧入口已经不能再保留。

## 交接纪律
以后只要跨窗口继续这条主线，默认继续沿用本文件更新，不要重新另起一套平行交接。
