# 总交接

## 一句话结论
当前主线已经明确收口到：**只做 sub2api 作为 MBclaw 的第一层地基**。活跃代码链已完成第一轮接入收口，下一窗口不要再扩散研究，只继续沿着 `参考文件/GitHub精品参考/sub2api/` 做最小必要魔改。

## 给下一窗口的唯一一句话
**强制把工作区锁到 `/tmp/openhands-sandboxes/MBclawZZ/`，不要用任何会话隔离副本路径；先读 `AGENTS.md` 和本文件，再只围绕 `参考文件/GitHub精品参考/sub2api/` 做地基魔改，严格延续提交 `3a2e4adb` 之后的工作，不要扩散到无关研究。**

## 当前状态
- 强制主工作区：`/tmp/openhands-sandboxes/MBclawZZ/`
- 禁止工作区：`/tmp/openhands-sandboxes/<session>/MBclawZZ/` 这类会话副本路径
- 当前默认分支：`main`
- 已推送 GitHub：是
- 最新提交：`3a2e4adb` — `refactor: route mother model entry through sub2api`
- 上一个关键提交：`51afe761` — `repo: upload full workspace state`
- 当前工作树状态：干净，可直接继续施工

## 当前主线口径
- **sub2api 是第一层地基。**
- **Mother 是建立在 sub2api 之上的第二层地基。**
- **控制面板只能建立在前两层地基之上，不反向定义地基。**
- **兼容方向固定为上层兼容下层，不让 sub2api 反向兼容 Mother 历史接口。**
- **当前任务只做 sub2api 地基，不继续发散做全局研究。**
- **TokenPool 只保留为历史参考，不再作为需要保留兼容壳的目标实现。**

## 这轮已经完成的关键进度
1. 已把 MBclaw 活跃模型入口链收口到 `server_app.sub2api_client.Sub2APIClient`。
2. 已把 `MBclaw-Server` 活跃入口模块中的 `app.*` 旧命名空间导入改成 `server_app.*`，避免运行时找不到包。
3. 已把根入口 `MBclaw-Server/main.py` 里的 `gateway_chat` 从直连 provider 改成走 `Sub2APIClient`。
4. 已把 `server_app/api.py`、`pipeline.py`、`agent.py`、`providers.py`、`db.py`、`models.py`、`memory.py`、`tools.py`、`server_app/main.py`、`server_app/admin/admin_api.py` 做了活跃链修正。
5. 已跑过基础验证：
   - `py_compile`
   - `import main`
   - `import server_app.main`
   - `import server_app.api`
   - `import server_app.agent`
   - `import server_app.pipeline`
   - `import server_app.providers`
   全部通过。
6. 已把这轮活跃链收口推到 `main`，对应提交 `3a2e4adb`。

## 下一窗口现在应该做什么
只做下面 3 件事：

1. **只看 `参考文件/GitHub精品参考/sub2api/` 本体。**
   重点是：
   - `backend/`
   - `frontend/`
   - `skills/sub2api-admin/`
   - `docs/`

2. **列出 MBclaw 对 sub2api 的最小魔改清单。**
   只围绕：
   - 统一模型入口
   - provider / key 治理
   - fallback / retry / cooldown / health / stats
   - 必要的管理接口

3. **开始直接改 sub2api 本体，而不是继续改外围说明文档。**
   只有在需要记录阶段结论时，才回写交接文档。

## 现在最该读的文件
按这个顺序读：

1. `AGENTS.md`
2. `6-交接工作区域/1-给AI的交接/总交接.md`
3. `4-总览端/3-对标矩阵/MBclaw_to_sub2api_专题整理大纲.md`
4. `4-总览端/3-对标矩阵/mbclaw-mother_与_sub2api_职责边界.md`
5. `参考文件/MBclaw参考配套文件/仓库/MBclaw-Server/server_app/sub2api_client.py`
6. `参考文件/MBclaw参考配套文件/仓库/MBclaw-Server/main.py`
7. `参考文件/GitHub精品参考/sub2api/README.md`
8. `参考文件/GitHub精品参考/sub2api/backend/`

如果只想最快接手，前 5 个就够了。

## 已确认可复用的代码入口
- `参考文件/MBclaw参考配套文件/仓库/MBclaw-Server/server_app/sub2api_client.py`
- `参考文件/MBclaw参考配套文件/仓库/MBclaw-Server/server_app/llm.py`
- `参考文件/MBclaw参考配套文件/仓库/MBclaw-Server/server_app/api.py`
- `参考文件/MBclaw参考配套文件/仓库/MBclaw-Server/server_app/pipeline.py`
- `参考文件/MBclaw参考配套文件/仓库/MBclaw-Server/server_app/agent.py`
- `参考文件/MBclaw参考配套文件/仓库/MBclaw-Server/server_app/providers.py`
- `参考文件/MBclaw参考配套文件/仓库/MBclaw-Server/main.py`

## 不要再做的事
- 不要再把任务扩成 Runtime Bible 全局研究。
- 不要再去扫一整片无关历史目录找“灵感”。
- 不要再把目标说成“继续兼容 TokenPool”。
- 不要在 `MBclaw-Server` 活跃链上重新引入直连 provider 的入口。
- 不要用会话隔离副本路径当主工作区。

## 风险点
- `参考文件/GitHub精品参考/sub2api/` 是大仓，容易扩散；下一窗口必须只盯和地基有关的目录与能力。
- `MBclaw-Server` 下面仍有历史 `TokenPool` / 调度残留目录，但这轮已经明确：**先不全仓清算，只继续推进 sub2api 地基本体。**
- 之前整仓上传曾被 GitHub secret scanning 卡住；如果后续再次大规模动参考仓快照，先留意是否会引入疑似凭据内容。

## 验收标准
下一窗口做完后，至少要能回答：
1. sub2api 作为地基，MBclaw 最小需要魔改哪几块。
2. 这些魔改分别落在 sub2api 的哪个目录。
3. Mother 现在经过什么入口调用 sub2api。
4. 哪些旧入口已经不能再保留。

## 交接纪律
以后只要跨窗口继续这条主线，默认继续沿用本文件更新，不要重新另起一套平行交接。
